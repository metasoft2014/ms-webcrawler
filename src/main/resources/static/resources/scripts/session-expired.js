var mainApp = angular.module('mainApp', []),
	SessionExpiredCtrl = function ($scope, $window, $location, $http) {

		/**
		 * Initialize
		 */
		(function () {

		})();

		/**
		 * Events
		 */
		(function () {

		})();

	};
mainApp.controller('SessionExpiredCtrl', SessionExpiredCtrl);